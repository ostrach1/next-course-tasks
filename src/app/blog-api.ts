"use server";

import { unstable_cache, revalidateTag } from "next/cache";

import { BlogEntry } from "./types";

const entries: BlogEntry[] = [];

// TODO: funkcja getEntries z użyciem unstable_cache

// TODO: funkcja addEntry z revalidateTag
